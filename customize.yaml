# Shades
cover.living_room_shades:
  icon: mdi:blinds

cover.dining_room_shades:
  icon: mdi:blinds

cover.master_bedroom_shades:
  icon: mdi:blinds

# Windows
binary_sensor.bonus_room_north_window:
  device_class: window
binary_sensor.bonus_room_south_window:
  device_class: window
binary_sensor.bonus_room_small_window:
  device_class: window
binary_sensor.dining_room_window:
  device_class: window
binary_sensor.guest_bedroom_left_window:
  device_class: window
binary_sensor.guest_bedroom_right_window:
  device_class: window
binary_sensor.kitchen_window:
  device_class: window
binary_sensor.laundry_window:
  device_class: window
binary_sensor.living_room_north_window:
  device_class: window
binary_sensor.living_room_south_window:
  device_class: window
binary_sensor.master_bathroom_window:
  device_class: window
binary_sensor.master_bedroom_window:
  device_class: window
binary_sensor.office_window:
  device_class: window

# Doors
binary_sensor.back_door_open:
  device_class: door

binary_sensor.office_closet_east_door:
  device_class: door

binary_sensor.office_closet_west_door:
  device_class: door

binary_sensor.hall_closet_door:
  device_class: door

# Leak Sensors
binary_sensor.dishwasher_leak:
  device_class: moisture

# People
person.sam:
  icon: mdi:human-male

# Misc
light.front_porch_stairs:
  icon: mdi:wall-sconce-flat

switch.office_space_heater_plug:
  icon: mdi:radiator
